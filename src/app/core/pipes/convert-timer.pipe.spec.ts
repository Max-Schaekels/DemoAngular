import { ConvertTimerPipe } from './convert-timer.pipe';

describe('ConvertTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
